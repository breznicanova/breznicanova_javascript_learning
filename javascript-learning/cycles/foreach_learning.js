const elements = ["button1", "field1", "tradio1"];

elements.forEach((element) => {
  console.log(element);
});

elements.forEach((element, index) => {
  console.log(`Element: ${element}, index: ${index}`);
});
